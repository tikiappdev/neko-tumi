import { Cat } from './Cat'; import type { CatKind } from './types';
export interface ResultScreenProps { score: number; floors: number; isNewBest: boolean; unlockedCat?: CatKind | null; onRetry: () => void; onTitle: () => void }
export function ResultScreen({ score, floors, isNewBest, unlockedCat, onRetry, onTitle }: ResultScreenProps) { return <main className={`screen result-screen ${isNewBest ? 'result-screen--best' : ''}`}><p className="result-screen__eyebrow">きょうの きろく</p><h1>{isNewBest ? '🎉 ベスト更新！' : 'よく がんばったね！'}</h1><Cat kind={isNewBest ? 'gold' : 'sleepy'}/><div className="result-score"><span>スコア</span><strong>{score}<small>てん</small></strong><b>🐾 {floors}段 つめたよ！</b></div>{unlockedCat && <div className="unlock-card" aria-live="polite"><Cat kind={unlockedCat}/><p><strong>あたらしい ねこ！</strong><br/>つぎのゲームから なかま入り</p></div>}<div className="screen__buttons"><button className="button button--primary" autoFocus onClick={onRetry}>↻ もういっかい！</button><button className="button button--secondary" onClick={onTitle}>⌂ タイトルにもどる</button></div></main>; }
