import { Cat } from './Cat'; import { SoundToggle } from './SoundToggle'; import type { BestRecord, CatKind } from './types';
export interface TitleScreenProps { best: BestRecord; soundEnabled: boolean; onToggleSound: () => void; onStart: () => void; onHowTo: () => void; featuredCat?: CatKind }
export function TitleScreen({ best, soundEnabled, onToggleSound, onStart, onHowTo, featuredCat = 'calico' }: TitleScreenProps) { return <main className="screen title-screen"><div className="screen__top"><SoundToggle enabled={soundEnabled} onToggle={onToggleSound}/></div><div className="title-cloud" aria-hidden="true">ふわふわ どこまで のぼれる？</div><h1><span>ねこ積み</span>タワー</h1><div className="title-mascot"><Cat kind={featuredCat}/><Cat kind="tabby" decorative/><Cat kind="white" decorative/></div><div className="best-card" aria-label={`ベストスコア ${best.score}、最高 ${best.floors}段`}><span>🏆 ベストきろく</span><strong>{best.score}<small>てん</small></strong><b>{best.floors}段</b></div><div className="screen__buttons"><button className="button button--primary" onClick={onStart}>▶ あそぶ！</button><button className="button button--secondary" onClick={onHowTo}>？ あそびかた</button></div></main>; }

